pre-commit:
  parallel: true
  commands:
    eslint:
      glob: "*.{ts,vue}"
      stage_fixed: true
      run: pnpm eslint --fix {staged_files}
    prettier:
      glob: "*.{scss,vue,ts,md}"
      stage_fixed: true
      run: pnpm prettier --write {staged_files}
    prettier-json:
      glob: "*.json"
      exclude: "openapi.json"
      stage_fixed: true
      run: pnpm prettier --write {staged_files}
    php-cs-fixer:
      glob: "*.php"
      exclude: "/gen/"
      stage_fixed: true
      env:
        PHP_CS_FIXER_IGNORE_ENV: "1"
      run: make php-cs-fixer && make test
    openapi:
      glob: "*Controller.php"
      run: make openapi && git add openapi.json openapi-full.json openapi-administration.json
    lint-appinfo:
      glob: "appinfo/info.xml"
      run: make lint-appinfo
